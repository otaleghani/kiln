---
title: Robots.txt
description: Custom description
---
# Robots.txt

Kiln auto-generates a `robots.txt` file on generation.

> [!warning]
> For the `robots.txt` file to be generated you need to add the flag `--url` on the `generate` command.


