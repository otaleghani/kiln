-- DwightAuto decomposition output
-- Request: The link preview feature now has a little problem where scrolling is mainteinded between one preview and another. It needs to reset when someone changes link to preview
-- Timestamp: Mon 09 Mar 2026 02:15:34 PM CET

The fix is a single line addition in `assets/link_preview.js` at line 121. After setting `tooltip.innerHTML = content`, we need to add:

```js
tooltip.scrollTop = 0;
```

This resets the scroll position to the top whenever new preview content is loaded, so each new link preview starts scrolled to the top regardless of where the previous preview was scrolled.

Want me to apply this edit?
