-- DwightAuto decomposition output
-- Request: The new search script works, but the display is not good: it hovers over the navbar and is illegible. We should rework it and make it like a hover panel. Maybe add a simple button for the search featu
-- Timestamp: Tue 03 Mar 2026 02:56:47 PM CET

Now I have the full picture. Let me formulate the decomposition.

<tasks>
<task order="1" title="Rework search.js to modal overlay pattern">
Rewrite `assets/search.js` to display search results in a centered modal overlay instead of a dropdown that hovers over the navbar.

**Current problem:** The search JS creates a `#search-results` dropdown positioned absolutely inside the sidebar search input's parent, which causes it to overlap the navbar and be illegible.

**Changes to `assets/search.js`:**
1. Remove the `getOrCreateDropdown(input)` function that creates an absolutely-positioned dropdown inside the input's parent.
2. Replace it with a modal-based approach. The search UI should be a fixed overlay (`#search-overlay`) centered on the page with a backdrop, similar to how the graph expand works in the existing codebase (see `.expanded` pattern in `assets/default_style_input.css`).
3. The overlay modal should contain:
   - A search input at the top (separate from the sidebar input)
   - A scrollable results list below it
   - Close on Escape key or clicking the backdrop
4. Replace `showResults(input, results, query)` to render results inside the modal panel instead of the old dropdown.
5. Replace `hideDropdown()` with a function that hides the modal overlay.
6. The `window.initFullSearch` function should:
   - Still fetch and build the inverted index on init
   - Create the modal overlay DOM structure once (a fixed backdrop + centered panel with an input + results container)
   - Listen for input events on the modal's own search input
   - Keep keyboard navigation (ArrowUp/ArrowDown/Enter/Escape) working
   - Remove the old `#navbar-search` input listener for full-text results (the sidebar input should only do sidebar filtering, handled by `initNavbarSearch` in `default_app.js`)
7. Export a `window.openSearchModal()` function that shows the overlay and focuses the modal input. This will be called from a sidebar button (added in next task).
8. Keep the `@feature:search` pragma on line 1.
9. Keep the `BASE_URL` template variable `"{{.BaseURL}}"` on line 3.
10. Keep `searchEntries()`, `snippet()`, `buildInvertedIndex()`, `fetchIndex()` functions unchanged.

**Also update `assets/shared.css`:**
Replace the existing search CSS block (lines 825-851, the `.search-results-dropdown` section and `.search-result-*` classes) with new styles for:
- `#search-overlay`: fixed inset-0, z-index 9999, flex centered, backdrop with `background: rgba(0,0,0,0.6)` and `backdrop-filter: blur(2px)`
- `#search-modal`: background `var(--bg-color)`, max-width 600px, width 90vw, max-height 70vh, border-radius 0.5rem, border `2px solid var(--accent-color)`, box-shadow, flex column, overflow hidden
- `#search-modal-input`: full-width input styled like the existing sidebar input (padding 0.75rem 1rem, font-size 1rem, border-bottom only, background transparent, color `var(--text-color)`)
- `#search-modal-results`: flex-1, overflow-y auto (scrollable results container)
- `.search-result-item`: display block, padding 0.75rem 1rem, text-decoration none, color `var(--text-color)`, border-bottom `1px solid var(--sidebar-border)`, cursor pointer
- `.search-result-item:hover`, `.search-result-item.highlighted`: background `var(--hover-color)`
- `.search-result-title`: font-weight 600
- `.search-result-folder`: color `var(--accent-color)`, font-size 0.75rem
- `.search-result-snippet`: color `var(--color-comment, #888)`, font-size 0.75rem, margin-top 0.125rem
- `#search-overlay.hidden`: display none

Verify: `go build ./...` should pass (this only touches JS/CSS assets, not Go code). Manually check the JS has no syntax errors by reviewing the file.
</task>

<task order="2" title="Add search button to default layout sidebar" depends_on="1">
Add a search button to the left sidebar in the default layout template that opens the search modal created in task 1.

**Edit `internal/templates/layout_default.templ`:**
1. In the left sidebar's `<div class="p-6 flex-1 overflow-y-auto flex flex-col gap-2">` section (around line 64), replace the existing search input block:
```
<div class="relative">
    <input type="text" id="navbar-search" placeholder="Search notes..." class="w-full p-2 rounded border border-sidebar-border text-sm"/>
</div>
```
with a search button that calls `window.openSearchModal()`:
```
<button
    id="search-button"
    onclick="if(window.openSearchModal)window.openSearchModal()"
    class="w-full p-2 rounded border border-sidebar-border text-sm text-left cursor-pointer bg-transparent hover:bg-hover transition-colors flex items-center gap-2"
>
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-4 h-4 opacity-60"><circle cx="11" cy="11" r="8"></circle><path d="m21 21-4.3-4.3"></path></svg>
    <span class="opacity-60">Search notes...</span>
</button>
```

2. Keep the `@feature:layouts` pragma on line 1.

**Edit `assets/default_app.js`:**
In the `window.initAll` function (around line 343), the current logic is:
```javascript
if (window.initFullSearch) {
    window.initFullSearch();
} else {
    window.initNavbarSearch();
}
```
Change this to just:
```javascript
if (window.initFullSearch) {
    window.initFullSearch();
}
```
Since the sidebar no longer has a `#navbar-search` input in the default layout, the fallback to `initNavbarSearch` is not needed for the default layout. Keep the `initNavbarSearch` function itself in the file (it may be used by simple layout or as fallback).

3. Also add a global keyboard shortcut in `default_app.js` — inside `window.initAll`, add:
```javascript
document.addEventListener("keydown", function(e) {
    if ((e.ctrlKey || e.metaKey) && e.key === "k") {
        e.preventDefault();
        if (window.openSearchModal) window.openSearchModal();
    }
});
```
This allows Ctrl+K / Cmd+K to open search, which is a common convention.

Verify: Run `cd /home/oliviero/Workspace/kiln && go generate ./internal/templates/... && go build ./...` to regenerate templ files and verify build passes.
</task>

<task order="3" title="Add search button and modal to simple layout" depends_on="2">
Add search support to the simple layout, which currently has no search at all.

**Edit `internal/templates/layout_simple.templ`:**
1. Add the search.js script tag in the `<head>` section, after the app.js line (line 30). Add:
```
<script defer src={ data.Site.BaseURL + "/search.js" }></script>
```
This matches how the default layout loads it (line 31 in `layout_default.templ`).

2. In the header `<div class="flex gap-2 items-center">` section (around line 41), add a search button BEFORE the `@ThemeToggler()` call. Add a new templ component call `@SimpleSearchButton()`.

**Edit `internal/templates/content.templ`:**
Add a new `SimpleSearchButton` templ component (add it near the other Simple* button components like `SimpleMenuButton`, `SimpleTOCButton`, `SimpleLocalGraphButton`):
```
templ SimpleSearchButton() {
	<div>
		<button
			id="search-button"
			aria-label="Search notes"
			title="Search"
			onclick="if(window.openSearchModal)window.openSearchModal()"
			class="cursor-pointer p-1 rounded hover:bg-hover transition-colors"
		>
			<svg
				xmlns="http://www.w3.org/2000/svg"
				width="24"
				height="24"
				viewBox="0 0 24 24"
				fill="none"
				stroke="currentColor"
				stroke-width="2"
				stroke-linecap="round"
				stroke-linejoin="round"
				class="h-5 w-5 text-foreground"
				id="search-icon"
			>
				<circle cx="11" cy="11" r="8"></circle>
				<path d="m21 21-4.3-4.3"></path>
			</svg>
		</button>
	</div>
}
```

**Edit `assets/simple_app.js`:**
1. In the `window.initToggles` function or at the end of the file, add search initialization similar to what default_app.js does. After the existing `window.initToggles();` call area, add a call to `window.initFullSearch()` if available. Look at how `simple_app.js` initializes — it uses `document.addEventListener("DOMContentLoaded", ...)`. In that handler (or create one), add:
```javascript
if (window.initFullSearch) {
    window.initFullSearch();
}
```
2. Also add the Ctrl+K / Cmd+K keyboard shortcut, same as in default_app.js.

Keep all existing `@feature:` pragmas on the files.

Verify: Run `cd /home/oliviero/Workspace/kiln && go generate ./internal/templates/... && go build ./...` to regenerate templ files and verify build passes.
</task>

<task order="4" title="Add Ctrl+K hint and polish search modal UX" depends_on="3">
Polish the search modal with a keyboard shortcut hint and empty state.

**Edit `assets/search.js`:**
1. In the modal DOM structure created by `initFullSearch`, add a subtle hint below the search input showing "Ctrl+K" (or "Cmd+K" on Mac) as the keyboard shortcut. This can be a small `<div>` with class `search-hint` showing the shortcut text.
2. Add an empty state message in the results container: when there are no results and the query is >= 2 chars, show "No results found" text.
3. Add an initial state message when the modal opens and the input is empty: show "Start typing to search..." as placeholder text in the results area.
4. Ensure that when the modal is opened via `openSearchModal()`, the input is cleared and focused, and any previous results are cleared.
5. Ensure clicking a search result link navigates and closes the modal.

**Edit `assets/shared.css`:**
Add styles for:
- `.search-hint`: text-align right, padding 0.25rem 1rem, font-size 0.7rem, color `var(--color-comment, #888)`, border-bottom `1px solid var(--sidebar-border)`
- `.search-empty`: padding 2rem, text-align center, color `var(--color-comment, #888)`, font-size 0.875rem

Keep all `@feature:` pragmas.

Verify: `go build ./...` should pass. Review the JS for syntax correctness.
</task>
</tasks>
