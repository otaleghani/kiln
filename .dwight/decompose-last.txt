-- DwightAuto decomposition output
-- Request: There are a couple of problems with the Incremental Builds feature. The first is that it fails to move some special files, like `_redirects`. Here's an error: 2026 10:32:24 ERRO Couldn't create new fi
-- Timestamp: Thu 12 Mar 2026 10:38:23 AM CET

Now I have a complete understanding. Here's the summary of issues:

**Bug 1: `_redirects` file fails** — During `Scan()`, the `_redirects` file is picked up by `NewFile()`. Since it has no extension (`ext == ""`), `GetPageOutputPath` treats it as a folder and tries to create `public/_redirects/index.html`. The `os.MkdirAll` fails because `_redirects` (the file) was already copied by `LoadRedirects()`, or the file system doesn't have a `_redirects` directory. The fix: skip special files (`_redirects`, `CNAME`, `favicon.ico`) during the vault scan since they're handled separately by `LoadRedirects()`, `LoadCname()`, `LoadFavicon()`.

**Bug 2: "Added to sitemap" uses `log.Println`** — In `sitemap.go:57`, the code uses the standard `log` package instead of the `o.log` slog logger, so it always prints and can't be suppressed. Should be `o.log.Debug(...)`.

**Bug 3: Ctrl+C doesn't exit** — In `dev.go`, the signal context cancels the watcher, but `server.Serve()` calls `http.ListenAndServe()` which blocks forever and doesn't listen to the context. The server needs to use `http.Server` with `Shutdown(ctx)`.

<tasks>
<task order="1" title="Fix sitemap logging to use slog Debug">
In `internal/obsidian/sitemap.go`, the `AddSitemapEntry` method on line 57 uses `log.Println("Added to sitemap", "url", webPath)` from the standard `log` package. This bypasses the structured logger and cannot be suppressed by log level configuration.

**Fix:**
1. Edit `internal/obsidian/sitemap.go`:
   - Remove the `"log"` import (line 6). The file should only import `"encoding/xml"`, `"os"`, `"path/filepath"`, `"strings"`, and `"time"`.
   - Change line 57 from `log.Println("Added to sitemap", "url", webPath)` to `o.log.Debug("Added to sitemap", "url", webPath)`.

**Verify:** Run `go build ./...` to confirm it compiles. Then run `go vet ./...` to check for issues.

This is a one-line fix plus import cleanup. The `o.log` field is already available on the `Obsidian` struct (used elsewhere in the same file, e.g., line 28 and 39).
</task>

<task order="2" title="Skip special files in vault scan">
The vault scanner in `internal/obsidian/obsidian.go` processes ALL files via `NewFile()`, including special files like `_redirects`, `CNAME`, and `favicon.ico`. These files have no extension, so `GetPageOutputPath` (in `internal/obsidian/paths.go`, line 106-108) treats them as folders and calls `os.MkdirAll(filepath.Join(outputDir, "_redirects"))`, which fails with `"mkdir public/_redirects: not a directory"` if the file already exists there.

These special files are already handled separately by `LoadRedirects()`, `LoadCname()`, and `LoadFavicon()` which are called at the end of `buildDefault()` (lines 435-448 of `internal/builder/builder_default.go`). They should be skipped during the vault scan.

**Fix:**
Edit `internal/obsidian/obsidian.go`, in the `Scan()` method. After the `_hidden_` prefix check (around line 475) and before the `info.IsDir()` check (line 478), add a check to skip special root-level files that are handled separately:

```go
// Skip special files handled by dedicated loaders (LoadRedirects, LoadCname, LoadFavicon)
if !info.IsDir() {
    base := filepath.Base(path)
    if base == "_redirects" || base == "CNAME" || base == "favicon.ico" {
        l.Debug("Skipping special file", "reason", "Handled by dedicated loader")
        return nil
    }
}
```

Insert this block between the `_hidden_` check and the `info.IsDir()` check.

**Verify:** Run `go build ./...` and `go vet ./...`. Then run `go test ./...` to ensure existing tests still pass.
</task>

<task order="3" title="Fix Ctrl+C graceful shutdown for dev server">
When using `kiln dev`, pressing Ctrl+C doesn't exit the process. The issue is in two files:

1. `internal/server/server.go` — The `Serve` function uses `http.ListenAndServe()` (line 109) which blocks forever and ignores signals. It needs to accept a `context.Context` and use `http.Server.Shutdown()` for graceful shutdown.

2. `internal/cli/dev.go` — The `runDev` function creates a signal context (line 157) but passes the server call `server.Serve(...)` on the main goroutine with no way to stop it.

**Fix `internal/server/server.go`:**

Change the `Serve` function signature from:
```go
func Serve(port, outputDir, baseURL string, log *slog.Logger) {
```
to:
```go
func Serve(ctx context.Context, port, outputDir, baseURL string, log *slog.Logger) {
```

Add `"context"` to the imports.

Replace the final lines (108-112):
```go
log.Info("Press Ctrl+C to stop")
if err := http.ListenAndServe(":"+port, nil); err != nil {
    log.Error("Server failed", "error", err)
    os.Exit(1)
}
```
with:
```go
log.Info("Press Ctrl+C to stop")
srv := &http.Server{Addr: ":" + port}
go func() {
    <-ctx.Done()
    srv.Shutdown(context.Background())
}()
if err := srv.ListenAndServe(); err != http.ErrServerClosed {
    log.Error("Server failed", "error", err)
    os.Exit(1)
}
log.Info("Server stopped")
```

**Fix `internal/cli/dev.go`:**

Change line 169 from:
```go
server.Serve(port, builder.OutputDir, localBaseURL, log)
```
to:
```go
server.Serve(ctx, port, builder.OutputDir, localBaseURL, log)
```

**Fix `internal/cli/serve.go`:**

The `serve.go` command also calls `server.Serve()`. Read the file to find the call and update it to pass a context. Since `serve` doesn't have a watcher, create a signal context similarly:
```go
ctx, stop := signal.NotifyContext(context.Background(), syscall.SIGINT, syscall.SIGTERM)
defer stop()
server.Serve(ctx, port, builder.OutputDir, localBaseURL, log)
```

Add the necessary imports (`"context"`, `"os/signal"`, `"syscall"`) to `serve.go`.

**Verify:** Run `go build ./...` and `go vet ./...`. Then run `go test ./...` to ensure existing tests still pass.
</task>
</tasks>
