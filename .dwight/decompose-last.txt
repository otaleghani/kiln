-- DwightAuto decomposition output
-- Request: There is an empty file in docs.md that needs to be filled with the internationalization feature. Read the content of the other features and create this one.
-- Timestamp: Tue 10 Mar 2026 05:28:29 PM CET

Now I have a thorough understanding of the i18n system and the documentation format. This is a single documentation task — writing the `docs/I18n.md` file. It doesn't need decomposition into multiple sub-tasks; it's a single file to write based on existing patterns. Let me produce the task decomposition anyway since the framework requires it.

<tasks>
<task order="1" title="Write I18n documentation">
Write the documentation file `docs/I18n.md` for the internationalization (i18n) feature. This is a documentation-only task — no Go code changes needed.

**Context**: The file `docs/I18n.md` is currently empty and needs to be filled with documentation about Kiln's internationalization feature. You must match the style and structure of the existing feature documentation files.

**Step 1**: Read these existing feature doc files to understand the exact format, tone, and structure:
- `docs/Features/Feed RSS.md`
- `docs/Features/Link Preview.md`
- `docs/Features/Image Optimization.md`

All docs follow this structure:
```
---
title: "Feature Name"
description: "One-sentence description for SEO/meta."
---
# Feature Name

Opening paragraph explaining what the feature does.

## Section 1
...
## Section N
...
## Limitations
- Bullet list of current limitations
```

**Step 2**: Read the i18n source code to understand the feature thoroughly:
- `internal/i18n/i18n.go` — Contains the `Labels` struct (21 fields for UI strings), a `languages` map with `"en"` and `"it"` entries, and `Resolve(lang string) *Labels` function that falls back to English for unknown codes.
- `internal/i18n/i18n_test.go` — Tests for English, Italian, unknown fallback, and all-fields-populated checks.

**Step 3**: Read how i18n is configured and wired:
- `internal/cli/commands.go` — Defines `FlagLang = "lang"`, `FlagLangShort = "g"`, `DefaultLang = "en"`
- `internal/cli/generate.go` — The `--lang` / `-g` flag is registered on the generate command, sets `builder.Lang`
- `internal/config/config.go` — The `Lang` field in the YAML config struct (`lang` YAML key)
- `internal/builder/templ_adapter.go` — Calls `i18n.Resolve(p.Site.Lang)` and passes `Labels` to templates
- `internal/templates/types.go` — `SiteData` struct has `Lang string` and `Labels *i18n.Labels`

**Step 4**: Write `docs/I18n.md` with the following sections and content:

1. **Frontmatter** — title: "Internationalization (i18n)", description about Kiln translating UI strings.

2. **Opening paragraph** — Kiln supports multiple languages for all user-facing UI strings (buttons, labels, tooltips). Set a language code and every built-in string adapts automatically.

3. **## How it works** — Explain the `Labels` struct, the `languages` registry, and `Resolve()` fallback behavior. Mention that labels are injected into HTML templates at build time AND passed to JavaScript via a hidden `#kiln-labels` element for client-side strings (search, copy button, etc.).

4. **## Configuration** — Show how to set the language:
   - CLI flag: `kiln generate --lang it` (or `-g it`)
   - Config file: `lang: it` in `kiln.yaml`
   - Default is `en` (English)

5. **## Supported languages** — Table listing language codes and names:
   | Code | Language |
   | `en` | English (default) |
   | `it` | Italian |

6. **## Translated strings** — Table listing all 21 Labels fields with their English values, grouped logically (search, navigation, content metadata, UI actions, etc.). Show the field name and the English default string.

7. **## Adding a new language** — Explain that currently new languages require adding an entry to the `languages` map in `internal/i18n/i18n.go`. There is no user-facing config file for custom translations. Describe the steps: add a new map entry with the language code key and fill in all 21 `Labels` fields.

8. **## Limitations** — Bullet list:
   - Only 2 languages built in (English and Italian)
   - No user-facing translation file — adding languages requires editing Go source
   - All 21 strings must be provided; there is no per-field fallback to English
   - No right-to-left (RTL) layout support
   - Custom Mode does not use the i18n labels — it is only for the default/simple themes

**Verify**: The file should be valid Markdown with proper frontmatter. Run: `head -5 docs/I18n.md` to confirm the frontmatter is present.
</task>
<task order="2" title="Review and verify I18n doc completeness" depends_on="1">
Review `docs/I18n.md` written in the previous task to ensure it is complete, accurate, and matches the project's documentation style.

**Step 1**: Read `docs/I18n.md` in full.

**Step 2**: Cross-reference the documented strings against the actual `Labels` struct in `internal/i18n/i18n.go`. The struct has exactly 21 fields:
- SearchPlaceholder, ToggleTheme, BackToTop, OnThisPage, TableOfContents, LocalGraph, GeneratedWith, PageNotFound, GoBackHome, Folder, Tag, Updated, Created, Words, MinRead, Copy, NoResults, Search, Navbar, Expand, LastModified

Verify that all 21 are mentioned in the documentation.

**Step 3**: Verify the CLI flag documentation matches `internal/cli/commands.go` (flag name `lang`, short flag `g`, default `en`).

**Step 4**: If any discrepancies are found (missing fields, wrong flag names, wrong defaults), fix them in `docs/I18n.md`.

**Verify**: Run `wc -l docs/I18n.md` to confirm the file has substantial content (should be 80+ lines). Run `grep -c '|' docs/I18n.md` to confirm tables are present.
</task>
</tasks>
