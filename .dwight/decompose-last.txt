-- DwightAuto decomposition output
-- Request: ### Image Optimization Pipeline
Resize and convert images to WebP/AVIF at build time. Could use `golang.org/x/image` (already a dependency) for resizing and shell out to `cwebp`/`avifenc` if available
-- Timestamp: Sun 08 Mar 2026 02:42:54 PM CET

Here is the complete decomposition, condensed:

<tasks>
<task order="1" title="Core image processing package">
Create `internal/imgopt` package with pure-Go image resizing and optional WebP/AVIF encoding via shell.

**Files to create:**
- `internal/imgopt/imgopt.go` — line 1: `// @feature:imgopt Image optimization: resize and convert images to WebP/AVIF at build time.`
- `internal/imgopt/imgopt_test.go` — line 1: `// @feature:imgopt`

**Types:** `Variant{Width int, Suffix string, Format string, OutPath string, WebPath string}`, `Result{Original string, Variants []Variant}`, `var ErrNoEncoder = errors.New("imgopt: encoder not available")`

**Functions:**
- `Resize(src image.Image, maxWidth int) image.Image` — `golang.org/x/image/draw` with `CatmullRom`. No-op if src width <= maxWidth.
- `EncodeWebP(w io.Writer, img image.Image, quality int) error` — Shell to `cwebp` via temp files. Return `ErrNoEncoder` if not on PATH.
- `EncodeAVIF(w io.Writer, img image.Image, quality int) error` — Shell to `avifenc`. Return `ErrNoEncoder` if unavailable.
- `DecodeImage(path string) (image.Image, string, error)` — `image.Decode` with `_ "image/jpeg"` and `_ "image/png"` registered.
- `ProcessImage(srcPath, outDir, webDir, baseName string, breakpoints []int) (*Result, error)` — Decodes, resizes at each breakpoint < original width, encodes WebP (if available) + original format, writes to outDir, returns Result with WebPaths.
- `IsOptimizable(ext string) bool` — true for `.png`, `.jpg`, `.jpeg` only.
- `DefaultBreakpoints() []int` — returns `[]int{1200, 800, 400}`.

**Tests (write FIRST):** `TestResize` (1600x900→800=800x450), `TestResize_SmallerThanTarget` (400x300→800=400x300), `TestDecodeImage` (round-trip PNG), `TestProcessImage` (1600x900 PNG, breakpoints [800,400], assert variants exist), `TestIsOptimizable` (table-driven).

**Verify:** `go test ./internal/imgopt/...`
</task>

<task order="2" title="Wire optimization into both build modes">
Integrate `imgopt` into both default and custom build pipelines.

**Files to edit:**
- `internal/builder/builder_default.go` — Add `ImageResults map[string]*imgopt.Result` to `DefaultSite` struct (line ~839). Initialize in constructor (line ~65). Replace static copy loop (lines 137-144): check `imgopt.IsOptimizable(file.Ext)`, call `imgopt.ProcessImage`, store in `site.ImageResults[file.WebPath]`; fallback to `obsidian.CopyFile` on error or non-optimizable files. Add `os.MkdirAll` before processing.
- `internal/builder/builder_custom.go` — Read fully to find `CustomSite` struct. Add `ImageResults map[string]*imgopt.Result`. Initialize where `&CustomSite{}` is constructed. In `parseStaticFiles()` (line 256), wrap `obsidian.CopyFile` (line 275) with same `imgopt.IsOptimizable` check and `ProcessImage` call.
- Both files: add import `"github.com/otaleghani/kiln/internal/imgopt"`.

**Verify:** `go test ./internal/builder/...`
</task>

<task order="3" title="Picture element rendering and full wiring">
Update markdown renderer to emit `<picture>` elements and wire `ImageResults` from builder through to renderer.

**Files to edit:**
- `internal/obsidian/markdown/wikilinks.go` — Add `ImageResults map[string]*imgopt.Result` field to `IndexResolver` (line 517). Modify `writeImage` (line 330): if `ImageResults` has entry for `src`, call new `writePicture` method; otherwise emit plain `<img loading="lazy">`. New method `writePicture` emits `<picture>` with `<source type="image/webp" srcset="...">` for webp variants, `<source srcset="...">` for original-format variants, and `<img src="..." loading="lazy">` fallback. Add `.avif` to `isImageFile` (line 480). Add import for `"fmt"` and `"github.com/otaleghani/kiln/internal/imgopt"`.
- `internal/obsidian/markdown/markdown.go` — Read fully. Add `ImageResults map[string]*imgopt.Result` to `ObsidianMarkdown`. Pass it to `IndexResolver` when constructing it in the `Render` method.
- `internal/builder/builder_default.go` — After image processing loop, before note rendering, set `site.Markdown.ImageResults = site.ImageResults`.
- `internal/builder/builder_custom.go` — After `parseStaticFiles()`, set markdown renderer's `ImageResults`.
- `internal/builder/utils.go` — Add `.avif` to `isImageExt` and `isAllowedExt`.

**Files to create:**
- `internal/obsidian/markdown/picture_test.go` — line 1: `// @feature:wikilinks`

**Tests (write FIRST):**
- `TestWriteImage_WithVariants` — `IndexResolver` with `ImageResults` for `/img/photo`, variants with webp 800w/400w + png 800w/400w. Assert output contains `<picture>`, `<source type="image/webp"`, `srcset=`, `loading="lazy"`, `</picture>`.
- `TestWriteImage_NoVariants` — src not in ImageResults. Assert plain `<img loading="lazy">`.
- `TestWriteImage_NilResults` — `ImageResults` is nil. Assert plain `<img loading="lazy">`.

**Verify:** `go test ./internal/obsidian/markdown/... && go test ./internal/builder/... && go test ./internal/imgopt/...`
</task>
</tasks>
