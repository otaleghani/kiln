-- DwightAuto decomposition output
-- Request: The incremental builds with `dev` command doesn't seem to work. Everytime that I change a single file the hole thing gets re-built. Also make the image optimization faster. Maybe make it concurrent wi
-- Timestamp: Thu 12 Mar 2026 09:52:13 AM CET



Continuing the task decomposition from where I left off:

<tasks>
<task order="1" title="Add IncrementalBuild to builder.go">
## Context
The `dev` command computes a `ChangeSet` via `watch.ComputeChangeSet` but discards it and calls `builder.Build(log)` which always calls `CleanOutputDir()` + full rebuild. This is the root cause.

## What to build

### Step 1: Write tests
Create `internal/builder/builder_test.go`:
```go
// @feature:builder Tests for incremental build logic.
package builder
```

Add tests:
- `TestShouldRebuildNilFilter`: Set `RebuildFilter = nil`, assert `shouldRebuild("any.md")` returns `true`.
- `TestShouldRebuildWithFilter`: Set `RebuildFilter` to `map[string]struct{}{"notes/a.md": {}}`, assert `shouldRebuild("notes/a.md")` returns `true` and `shouldRebuild("notes/b.md")` returns `false`.

### Step 2: Add RebuildFilter and shouldRebuild
Edit `internal/builder/builder.go`. Add a package-level var:
```go
var RebuildFilter map[string]struct{}
```

Edit `internal/builder/utils.go` (has: `Init`, `CleanOutputDir`, `isImageExt`, `isAllowedExt`). Add:
```go
func shouldRebuild(relPath string) bool {
    if RebuildFilter == nil {
        return true
    }
    _, ok := RebuildFilter[relPath]
    return ok
}
```

### Step 3: Add IncrementalBuild function
Edit `internal/builder/builder.go`. Add:
```go
func IncrementalBuild(log *slog.Logger, rebuild []string, remove []string) {
    for _, relPath := range remove {
        outPath := filepath.Join(OutputDir, relPath)
        if strings.HasSuffix(relPath, ".md") {
            outPath = strings.TrimSuffix(outPath, ".md")
            if FlatUrls {
                outPath += ".html"
            } else {
                outPath = filepath.Join(outPath, "index.html")
            }
        }
        os.Remove(outPath)
    }

    RebuildFilter = make(map[string]struct{}, len(rebuild))
    for _, relPath := range rebuild {
        RebuildFilter[relPath] = struct{}{}
    }
    defer func() { RebuildFilter = nil }()

    switch Mode {
    case "custom":
        log.Info("Incremental build (custom mode)")
        buildCustom(log)
    default:
        log.Info("Incremental build (default mode)")
        buildDefault(log)
    }
}
```

Add `"path/filepath"`, `"strings"`, `"os"` to imports.

### Step 4: Apply filter in buildDefault
Edit `internal/builder/builder_default.go`. Add `if !shouldRebuild(file.RelPath) { continue }` as the first line inside these loops:
- Static files loop (line 142, `for _, file := range staticFiles`)
- Canvas pages loop (line 198, `for _, file := range canvasPages`)
- Note pages loop (line 232, `for _, note := range notePages`)
- Base pages loop (line 215, `for _, base := range basePages`) — use `base.File.RelPath`

Do NOT filter folder rendering (folders are cheap and may reference changed files).

### Step 5: Wire into dev.go
Edit `internal/cli/dev.go` line 136. Change `builder.Build(log)` to `builder.IncrementalBuild(log, cs.Rebuild, cs.Remove)`.

### Step 6: Verify
Run `go test ./internal/builder/...` and `go build ./cmd/kiln/`.
</task>

<task order="2" title="Update dep graph after incremental rebuild" depends_on="1">
## Context
The dependency graph is built once at startup but never updated. When files change links, the graph becomes stale.

## What to build

### Step 1: Write test
Edit `internal/watch/depgraph_test.go` (has: `TestAddEdge`, `TestRemoveSource`, `TestDependents`, `TestBuildFromFiles`, etc.). Read it first. Add:
```go
func TestUpdateFiles(t *testing.T) {
    g := NewDepGraph()
    g.AddEdge("a.md", "b")
    
    files := []*obsidian.File{
        {RelPath: "a.md", Ext: ".md", Links: []string{"[[c]]"}},
    }
    g.UpdateFiles(files)
    
    if deps := g.Dependents("b"); len(deps) != 0 {
        t.Errorf("expected no dependents for b, got %v", deps)
    }
    if deps := g.Dependents("c"); len(deps) != 1 || deps[0] != "a.md" {
        t.Errorf("expected [a.md] for c, got %v", deps)
    }
}
```

### Step 2: Add UpdateFiles method
Edit `internal/watch/depgraph.go` (has: `NewDepGraph`, `AddEdge`, `RemoveSource`, `Dependents`, `BuildFromFiles`, `parseTarget`). Add:
```go
func (g *DepGraph) UpdateFiles(files []*obsidian.File) {
    for _, f := range files {
        if f.Ext != ".md" {
            continue
        }
        g.RemoveSource(f.RelPath)
        for _, link := range f.Links {
            target := parseTarget(link)
            if target == "" {
                continue
            }
            g.AddEdge(f.RelPath, target)
        }
    }
}
```

### Step 3: Update dev.go OnRebuild callback
Edit `internal/cli/dev.go`. After `builder.IncrementalBuild(log, cs.Rebuild, cs.Remove)` (the line changed in task 1), add a vault re-scan and graph update:
```go
// Refresh dependency graph for changed files
vault := obsidian.New(
    obsidian.WithInputDir(inputDir),
    obsidian.WithOutputDir(outputDir),
    obsidian.WithBaseURL(baseURL),
    obsidian.WithFlatURLs(flatUrls),
    obsidian.WithLogger(log),
)
if err := vault.Scan(); err != nil {
    log.Error("failed to rescan vault", "err", err)
    return nil
}
graph.UpdateFiles(vault.Vault.Files)
```

Note: the `vault` variable shadows the outer one — this is intentional since we need fresh file data.

### Step 4: Verify
Run `go test ./internal/watch/...` and `go build ./cmd/kiln/`.
</task>

<task order="3" title="Concurrent image optimization with goroutines" depends_on="1">
## Context
Image optimization processes images sequentially in a `for` loop. Each image decodes, resizes at 3 breakpoints, and shells out to `cwebp`/`avifenc`. No concurrency.

## What to build

### Step 1: Write test
Edit `internal/imgopt/imgopt_test.go` (has: helpers `newTestImage`/`savePNG`, tests `TestResize`, `TestResize_SmallerThanTarget`, `TestDecodeImage`, `TestProcessImage`, `TestIsOptimizable`). Add `"fmt"` to imports. Add:
```go
func TestProcessImages(t *testing.T) {
    dir := t.TempDir()
    outDir := filepath.Join(dir, "out")
    os.MkdirAll(outDir, 0o755)

    var jobs []ImageJob
    for i := 0; i < 4; i++ {
        name := fmt.Sprintf("img%d", i)
        path := filepath.Join(dir, name+".png")
        savePNG(t, path, newTestImage(1600, 900))
        jobs = append(jobs, ImageJob{
            SrcPath:  path,
            OutDir:   outDir,
            WebDir:   "/images",
            BaseName: name,
            WebPath:  "/images/" + name + ".png",
        })
    }

    results := ProcessImages(jobs, DefaultBreakpoints(), 4)
    if len(results) != 4 {
        t.Fatalf("expected 4 results, got %d", len(results))
    }
    for _, job := range jobs {
        if results[job.WebPath] == nil {
            t.Errorf("nil result for %s", job.WebPath)
        }
    }
}
```

### Step 2: Add ImageJob and ProcessImages
Edit `internal/imgopt/imgopt.go` (has: `Variant`, `Result`, `ErrNoEncoder`, functions `DefaultBreakpoints`, `IsOptimizable`, `DecodeImage`, `Resize`, `EncodeWebP`, `EncodeAVIF`, `ProcessImage`, `writeImage`, `writeEncoded`). Add `"sync"` to imports. Add at end:
```go
type ImageJob struct {
    SrcPath  string
    OutDir   string
    WebDir   string
    BaseName string
    WebPath  string
}

func ProcessImages(jobs []ImageJob, breakpoints []int, maxWorkers int) map[string]*Result {
    if maxWorkers < 1 {
        maxWorkers = 1
    }
    type entry struct {
        webPath string
        result  *Result
    }
    jobCh := make(chan ImageJob, len(jobs))
    resultCh := make(chan entry, len(jobs))
    var wg sync.WaitGroup
    for i := 0; i < maxWorkers; i++ {
        wg.Add(1)
        go func() {
            defer wg.Done()
            for job := range jobCh {
                r, err := ProcessImage(job.SrcPath, job.OutDir, job.WebDir, job.BaseName, breakpoints)
                if err != nil {
                    continue
                }
                resultCh <- entry{webPath: job.WebPath, result: r}
            }
        }()
    }
    for _, job := range jobs {
        jobCh <- job
    }
    close(jobCh)
    go func() { wg.Wait(); close(resultCh) }()
    results := make(map[string]*Result, len(jobs))
    for e := range resultCh {
        results[e.webPath] = e.result
    }
    return results
}
```

### Step 3: Verify imgopt tests
Run `go test ./internal/imgopt/...`.

### Step 4: Update builder_default.go
Edit `internal/builder/builder_default.go`. Add `"runtime"` to imports. Replace the static files loop (lines 141-164) with:
1. First pass: collect `imgopt.ImageJob` slice and copy all files.
2. Call `imgopt.ProcessImages(jobs, imgopt.DefaultBreakpoints(), runtime.NumCPU())`.
3. Merge returned map into `site.ImageResults`.

The new code replacing lines 141-164:
```go
log.Info("Copying static assets...")
var imgJobs []imgopt.ImageJob
for _, file := range staticFiles {
    if !shouldRebuild(file.RelPath) {
        continue
    }
    l := log.With("file", file.Path)
    if err := os.MkdirAll(filepath.Dir(file.OutPath), 0755); err != nil {
        l.Error("Couldn't create output directory", "error", err)
        continue
    }
    if imgopt.IsOptimizable(file.Ext) {
        imgJobs = append(imgJobs, imgopt.ImageJob{
            SrcPath:  file.Path,
            OutDir:   filepath.Dir(file.OutPath),
            WebDir:   filepath.Dir(file.WebPath),
            BaseName: file.Name,
            WebPath:  file.WebPath,
        })
    }
    if copyErr := obsidian.CopyFile(file.Path, file.OutPath); copyErr != nil {
        l.Error("Couldn't copy file", "error", copyErr)
    }
}
for k, v := range imgopt.ProcessImages(imgJobs, imgopt.DefaultBreakpoints(), runtime.NumCPU()) {
    site.ImageResults[k] = v
}
```

### Step 5: Update builder_custom.go
Edit `internal/builder/builder_custom.go`. In `parseStaticFiles()` (lines 257-296), apply the same pattern: collect `ImageJob` slice, call `ProcessImages` after the loop, merge results. Add `"runtime"` to imports.

### Step 6: Verify
Run `go test ./internal/imgopt/... ./internal/builder/...` and `go build ./cmd/kiln/`.
</task>
</tasks>
