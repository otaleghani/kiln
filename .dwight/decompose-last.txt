-- DwightAuto decomposition output
-- Request: I would like to add a new thing in the kiln.yaml configuration file, which is the accent color. We have different themes to generate the websites, but I decided which accent color to use. I would like
-- Timestamp: Tue 10 Mar 2026 06:26:55 PM CET

Now I have a comprehensive understanding of the codebase. Let me trace the full flow:

1. `kiln.yaml` → `config.Config` struct (parsed via yaml tags)
2. CLI flags + config → `builder.*` globals
3. `builder.ResolveTheme(themeName, fontName)` → `*Theme` with `Light.Accent` and `Dark.Accent` set from theme registry
4. The `Accent` field flows through `toTemplTheme` → `templates.ThemeColors.Accent` → CSS `--accent-color` variable via `writeColorVars()`

The user wants to let users pick which palette color (Red, Orange, Yellow, Green, Blue, Purple, Cyan) to use as the accent color. This means:
- A new `accent-color` field in `kiln.yaml` 
- After resolving the theme, if the user specified an accent color, override `theme.Light.Accent` and `theme.Dark.Accent` with the corresponding palette color from the theme
- The existing CSS variable system (`--accent-color`) already propagates automatically

<tasks>
<task order="1" title="Add accent-color to Config and tests">
Add the `accent-color` field to the config system and update tests.

**What to do:**

1. Edit `internal/config/config.go`:
   - Add `AccentColor string \`yaml:"accent-color"\`` field to the `Config` struct (after `Lang string \`yaml:"lang"\``)
   - Add a `"accent-color"` case to the `ValueOr` switch statement that returns `c.AccentColor`

2. Edit `internal/config/config_test.go`:
   - In `TestLoad_ValidFile`: add `accent-color: blue` to the test YAML content, and add an assertion: `if cfg.AccentColor != "blue" { t.Errorf(...) }`
   - In `TestLoad_PartialFile`: add assertion that `cfg.AccentColor` is empty string (zero value)
   - In `TestLoad_EmptyFile`: add assertion that `cfg.AccentColor` is empty string
   - In `TestValueOr_OnlyOverridesEmpty`: add a test case for `accent-color` — set `AccentColor: "red"` in the Config struct, verify `ValueOr("accent-color", "")` returns `"red"`, and verify that when AccentColor is empty, `ValueOr("accent-color", "")` returns `""`

3. Edit `internal/config/config_integration_test.go`:
   - In `TestConfigIntegration_FullPipeline`: add `accent-color: green` to the YAML content and assert `cfg.AccentColor == "green"`
   - Add a `ValueOr` assertion: `cfg.ValueOr("accent-color", "") == "green"`

**Verify:** `go test ./internal/config/...`
</task>

<task order="2" title="Add AccentColorName to builder and CLI wiring" depends_on="1">
Wire the new `accent-color` config field through CLI flags and builder globals, and add the accent color override logic to `ResolveTheme`.

**What to do:**

1. Edit `internal/cli/commands.go`:
   - Add a default constant: `DefaultAccentColor = ""` (empty string means "use the theme's built-in accent")
   - Add flag name constants: `FlagAccentColor = "accent-color"` and `FlagAccentColorShort = "a"`
   - Add a CLI variable: `accentColor string`

2. Edit `internal/cli/generate.go`:
   - Add a new flag registration in `init()`:
     ```go
     cmdGenerate.Flags().StringVarP(&accentColor, FlagAccentColor, FlagAccentColorShort, DefaultAccentColor, "Accent color from theme palette (red, orange, yellow, green, blue, purple, cyan)")
     ```
   - In `runGenerate`, add `applyStringFlag(cmd, FlagAccentColor, &accentColor, cfg, DefaultAccentColor)` after the existing applyStringFlag calls
   - Add `builder.AccentColorName = accentColor` after the existing builder assignments

3. Edit `internal/builder/builder.go`:
   - Add `AccentColorName string` to the `var` block (the package-level globals), with comment `// Accent color override (palette color name)`

4. Edit `internal/builder/themes.go`:
   - Change `ResolveTheme` signature to: `func ResolveTheme(themeName, fontName, accentColorName string, log *slog.Logger) *Theme`
   - Inside `ResolveTheme`, after resolving the font, add accent color override logic:
     ```go
     if accentColorName != "" {
         overrideAccentColor(theme, accentColorName, log)
     }
     ```
   - Add a new function `overrideAccentColor(theme *Theme, colorName string, log *slog.Logger)` that:
     - Creates a map `colorMap` of palette color names to getter functions, or use a switch statement
     - For light theme: looks up `colorName` (lowercased) against the palette colors (Red, Orange, Yellow, Green, Blue, Purple, Cyan) of `theme.Light` and sets `theme.Light.Accent` to that value
     - For dark theme: does the same with `theme.Dark`
     - If the color name is not recognized, logs a warning and leaves the accent unchanged
     - Implementation approach using a helper:
       ```go
       func paletteColor(c *ThemeColors, name string) (string, bool) {
           switch strings.ToLower(name) {
           case "red":    return c.Red, true
           case "orange": return c.Orange, true
           case "yellow": return c.Yellow, true
           case "green":  return c.Green, true
           case "blue":   return c.Blue, true
           case "purple": return c.Purple, true
           case "cyan":   return c.Cyan, true
           default:       return "", false
           }
       }
       ```

5. Update callers of `ResolveTheme` — there are two:
   - `internal/builder/builder_default.go` line 31: change `ResolveTheme(ThemeName, FontName, log)` to `ResolveTheme(ThemeName, FontName, AccentColorName, log)`
   - `internal/builder/builder_custom.go`: search for `ResolveTheme` call and update similarly

**Verify:** `go test ./...`
</task>

<task order="3" title="Update kiln.yaml with accent-color example" depends_on="2">
Update the project's `kiln.yaml` to document the new `accent-color` option.

**What to do:**

1. Edit `kiln.yaml` at the project root — add a commented-out `accent-color` line after the `theme` line:
   ```yaml
   theme: "dracula"
   # accent-color: blue
   font: inter
   ```
   The comment serves as documentation for users. The available values are: red, orange, yellow, green, blue, purple, cyan. When unset, the theme's default accent color is used.

**Verify:** `go test ./...` (ensure nothing broke)
</task>
</tasks>
