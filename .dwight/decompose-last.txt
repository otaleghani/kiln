-- DwightAuto decomposition output
-- Request: Create in the right sidebar a backlink panel, so a dedicated section where we show all the incoming links to the current page.
-- Timestamp: Wed 11 Mar 2026 02:51:39 PM CET

Now I have a complete understanding. The backlinks are stored as `[]string` containing `[[NoteName]]` format. To render clickable links in the backlink panel, we need to resolve each backlink name to its `WebPath` using the `FileIndex`. 

Looking at the data flow, `PageData.File` already contains `File.Backlinks`, but we need resolved backlinks (name + URL pairs). Let me now formulate the task decomposition.

<tasks>
<task order="1" title="Add Backlinks i18n label and config flag">
Add the `DisableBacklinks` configuration option and the `Backlinks` i18n label, following the exact same pattern as `DisableTOC` and `DisableLocalGraph`.

Files to edit:

1. **`internal/i18n/i18n.go`** — Add a `Backlinks` field to the `Labels` struct (after `LocalGraph`). Set the English value to `"Backlinks"` and the Italian value to `"Backlinks"` (it's the same term in both languages). The struct currently has fields like `SearchPlaceholder`, `ToggleTheme`, `BackToTop`, `OnThisPage`, `TableOfContents`, `LocalGraph`, etc.

2. **`internal/config/config.go`** — Add `DisableBacklinks bool \`yaml:"disable-backlinks"\`` to the `Config` struct (after `DisableLocalGraph`). Add a case `"disable-backlinks": return c.DisableBacklinks` to the `BoolOr` method's switch statement.

3. **`internal/cli/commands.go`** — Add `DefaultDisableBacklinks = false` to the defaults const block (after `DefaultDisableLocalGraph`). Add `FlagDisableBacklinks = "disable-backlinks"` to the flag names const block (after `FlagDisableLocalGraph`). Add `disableBacklinks bool` to the var block (after `disableLocalGraph`).

4. **`internal/cli/generate.go`** — In `init()`, add a flag registration: `cmdGenerate.Flags().BoolVar(&disableBacklinks, FlagDisableBacklinks, DefaultDisableBacklinks, "Disables the Backlinks panel on the right sidebar.")`. In `runGenerate`, add `applyBoolFlag(cmd, FlagDisableBacklinks, &disableBacklinks, cfg, DefaultDisableBacklinks)` and `builder.DisableBacklinks = disableBacklinks`.

5. **`internal/cli/init.go`** — Add `# disable-backlinks: false` to the scaffold YAML template string, after the `# disable-local-graph: false` line.

6. **`internal/builder/builder.go`** — Add `DisableBacklinks bool` to the package-level var block (after `DisableLocalGraph`).

All files already exist — only edit, don't create new files. Verify with: `go build ./...`
</task>

<task order="2" title="Wire DisableBacklinks through builder and template types">
Wire the `DisableBacklinks` flag from the builder through the templ adapter into the template's `SiteData`, and add a `Backlink` struct to the template types for resolved backlinks.

Files to edit:

1. **`internal/templates/types.go`** — Add `DisableBacklinks bool` to the `SiteData` struct (after `DisableTOC`). The struct currently has: `BaseURL`, `SiteName`, `Theme`, `NavbarRoot`, `DisableLocalGraph`, `DisableTOC`, `FlatURLs`, `Lang`, `Labels`. Also add a new `Backlink` struct:
```go
type Backlink struct {
    Name    string
    WebPath string
}
```
Add a `Backlinks []Backlink` field to the `PageData` struct (after the `Meta` field).

2. **`internal/builder/builder_default.go`** — Add `DisableBacklinks bool` to the `DefaultSite` struct (after `DisableTOC`, around line 872). The struct currently has fields: `BaseURL`, `SiteName`, `Theme`, `Layout`, `NavbarRoot`, `Markdown`, `Minifier`, `DisableLocalGraph`, `DisableTOC`, `FlatURLs`, `OGFontFace`, `Lang`, `log`, `Obsidian`, `ImageResults`. In the `buildDefault` function (around line 66-83), add `DisableBacklinks: DisableBacklinks,` to the `DefaultSite` initialization (after `DisableTOC: DisableTOC,`).

3. **`internal/builder/templ_adapter.go`** — In the `toTemplPageData` function, add `DisableBacklinks: p.Site.DisableBacklinks,` to the `templates.SiteData` initialization (after `DisableTOC`). Also, inside the `if p.IsNote && p.File != nil` block (after the `data.Meta` assignment), add backlink resolution:
```go
if !p.Site.DisableBacklinks && len(p.File.Backlinks) > 0 {
    for _, bl := range p.File.Backlinks {
        name := strings.TrimPrefix(bl, "[[")
        name = strings.TrimSuffix(name, "]]")
        if files, ok := p.Site.Obsidian.Vault.FileIndex[name]; ok && len(files) > 0 {
            data.Backlinks = append(data.Backlinks, templates.Backlink{
                Name:    name,
                WebPath: files[0].WebPath,
            })
        }
    }
}
```
This will require adding `"strings"` to the imports if not already present. The `DefaultSitePageData` struct has a `Site *DefaultSite` field which already has the `Obsidian` field with `Vault.FileIndex`.

4. **`internal/builder/builder_default.go`** (same file as step 2) — Ensure that `DefaultSitePageData` is accessible. Check its definition — it likely has `Site *DefaultSite` field which gives access to `Site.Obsidian.Vault.FileIndex`.

Verify with: `go build ./...`
</task>

<task order="3" title="Add BacklinksPanel templ component for default layout">
Create the `BacklinksPanel` templ component and add it to the right sidebar in the default layout. Follow the exact same pattern as `TOCPanel`.

Files to edit:

1. **`internal/templates/content.templ`** — After the `TOCPanel` component (around line 397), add a new `BacklinksPanel` templ component:
```templ
templ BacklinksPanel(data *PageData) {
    if !data.Site.DisableBacklinks && len(data.Backlinks) > 0 {
        <div id="backlinks-container" hx-swap-oob="true">
            <header class="flex justify-between items-center py-2 border-b border-sidebar-border mb-4">
                <span class="font-bold">{ data.Site.Labels.Backlinks }</span>
            </header>
            <ul class="flex flex-col gap-1">
                for _, bl := range data.Backlinks {
                    <li>
                        <a
                            href={ templ.SafeURL(bl.WebPath) }
                            class="text-sm hover:text-accent transition-colors"
                        >
                            { bl.Name }
                        </a>
                    </li>
                }
            </ul>
        </div>
    }
}
```

2. **`internal/templates/layout_default.templ`** — In the right sidebar `<div id="right-sidebar-content">` section (around line 146-148), add `@BacklinksPanel(data)` after `@TOCPanel(data)`. The current code is:
```templ
<div id="right-sidebar-content" class="p-4 flex-1 overflow-y-auto flex flex-col gap-8">
    @TOCPanel(data)
</div>
```
Change it to:
```templ
<div id="right-sidebar-content" class="p-4 flex-1 overflow-y-auto flex flex-col gap-8">
    @TOCPanel(data)
    @BacklinksPanel(data)
</div>
```

Also update the right sidebar visibility condition (line 122). Currently it's:
```templ
if !data.Site.DisableLocalGraph || !data.Site.DisableTOC {
```
Change it to:
```templ
if !data.Site.DisableLocalGraph || !data.Site.DisableTOC || !data.Site.DisableBacklinks {
```

And the same condition on line 108 (the toggle button visibility):
```templ
if !data.Site.DisableLocalGraph || !data.Site.DisableTOC {
```
Change to:
```templ
if !data.Site.DisableLocalGraph || !data.Site.DisableTOC || !data.Site.DisableBacklinks {
```

And the condition on line 105 (when to show graph button in main header):
```templ
if data.Site.DisableLocalGraph && data.Site.DisableTOC {
```
Change to:
```templ
if data.Site.DisableLocalGraph && data.Site.DisableTOC && data.Site.DisableBacklinks {
```

3. Run `go generate ./internal/templates/...` or `templ generate` to regenerate the `_templ.go` files from the `.templ` sources. If `templ` is not available in PATH, try `go run github.com/a-h/templ/cmd/templ@latest generate`.

Verify with: `go build ./...`
</task>

<task order="4" title="Add BacklinksPanel to simple layout">
Add backlinks support to the simple layout, following the exact same pattern as `SimpleTOCContainer` and `SimpleTOCButton`.

Files to edit:

1. **`internal/templates/content.templ`** — After the `SimpleLocalGraphContainer` component (around line 539), add two new components:

A `SimpleBacklinksButton` component (follow the pattern of `SimpleTOCButton` and `SimpleLocalGraphButton`):
```templ
templ SimpleBacklinksButton(labels *i18n.Labels) {
    <div>
        <button
            id="backlinks-button"
            aria-label={ labels.Backlinks }
            title={ labels.Backlinks }
            class="cursor-pointer p-1 rounded hover:bg-hover transition-colors"
        >
            <svg
                xmlns="http://www.w3.org/2000/svg"
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="w-5 h-5 text-foreground"
                id="backlinks-icon"
            >
                <path d="M9 17H7A5 5 0 0 1 7 7h2"></path>
                <path d="M15 7h2a5 5 0 1 1 0 10h-2"></path>
                <line x1="8" x2="16" y1="12" y2="12"></line>
            </svg>
        </button>
    </div>
}
```

A `SimpleBacklinksContainer` component (follow pattern of `SimpleTOCContainer`):
```templ
templ SimpleBacklinksContainer(data *PageData) {
    <div
        class="fixed inset-0 z-40 bg-background pb-4 hidden overflow-scroll p-4 md:p-0"
        id="backlinks-wrapper"
    >
        <div class="max-w-prose mx-auto w-full rounded-xl border border-sidebar-border p-6 mt-20 md:mt-24">
            if !data.Site.DisableBacklinks && len(data.Backlinks) > 0 {
                <div id="backlinks-container">
                    <header class="flex justify-between items-center py-2 border-b border-sidebar-border mb-4">
                        <span class="font-bold">{ data.Site.Labels.Backlinks }</span>
                    </header>
                    <ul class="flex flex-col gap-1">
                        for _, bl := range data.Backlinks {
                            <li>
                                <a
                                    href={ templ.SafeURL(bl.WebPath) }
                                    class="text-sm hover:text-accent transition-colors"
                                >
                                    { bl.Name }
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            }
        </div>
    </div>
}
```

2. **`internal/templates/layout_simple.templ`** — In the header buttons section (around line 56-58), add after the local graph button:
```templ
if !data.Site.DisableBacklinks && len(data.Backlinks) > 0 {
    @SimpleBacklinksButton(data.Site.Labels)
}
```

In the modal containers section (around line 79-81), add:
```templ
if !data.Site.DisableBacklinks {
    @SimpleBacklinksContainer(data)
}
```

3. **`assets/simple_app.js`** — Add the backlinks panel to the `window._panels` array. Currently it has 3 panels (menu, local-graph, toc). Add a 4th:
```js
{ btn: "backlinks-button", wrapper: "backlinks-wrapper", icon: "backlinks-icon" },
```
Also add a named toggle function:
```js
window.toggleBacklinks = () =>
  window._togglePanel(window._panels[3]);
```

4. Run `templ generate` (or `go run github.com/a-h/templ/cmd/templ@latest generate`) to regenerate `_templ.go` files.

Verify with: `go build ./...`
</task>

<task order="5" title="Regenerate templ files and run tests">
Regenerate all templ output files and run the full test suite to verify everything compiles and tests pass.

Steps:
1. Run `go run github.com/a-h/templ/cmd/templ@latest generate` from the project root to regenerate all `*_templ.go` files from the `.templ` sources. If this fails, check the templ version in go.mod (`github.com/a-h/templ@v0.3.1001`) and use the matching version.

2. Run `go build ./...` to verify compilation.

3. Run `go test ./...` to run all tests.

4. If any tests fail related to the config (e.g., `config_test.go` or `config_integration_test.go`), check if the test YAML fixtures need updating with the new `disable-backlinks` field. The existing tests use inline YAML strings — the new field defaults to `false` so existing tests should still pass without changes.

5. If templ generation fails, check the `.templ` files for syntax errors. Common issues:
   - Missing imports in templ files
   - Incorrect templ syntax (e.g., using Go-style `if len(slice) > 0` is valid in templ)
   - Make sure `templ.SafeURL` is used for href attributes

Verify with: `go test ./...` — all tests must pass.
</task>
</tasks>
